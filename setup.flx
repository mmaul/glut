include "PKGTOOL/pkgtool";
BUILD_LIKE = Lib;
 
open PkgTool;
open Filename;

instance PkgTool {

  proc build () {
    create_config("glut",list("GL/glut.h"),
      list("libglut"+#Config::config.EXT_SHLIB),list("gl","glu"));
    default_build();
  }
}

SetupTool::run();
